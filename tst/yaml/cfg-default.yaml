# $schema: ../../schema/cfg-2.0.0.yaml
ARRANGE: ROW
ALIGN: TOP
ORDER:
SCALING: TRUE
SCALE_ROUND_TO: 0.125
SCALE_ROUND_STRATEGY: NEAREST
AUTO_SCALE: TRUE
AUTO_SCALE_DPI: 96
AUTO_SCALE_MIN: 1
AUTO_SCALE_MAX: -1
SCALE:
MODE:
TRANSFORM:
VRR_OFF:
CALLBACK_CMD: notify-send "way-displays ${CALLBACK_LEVEL}" "${CALLBACK_MSG}"
LAPTOP_LID_MONITOR: TRUE
DISABLED:
  '!^eDP-[0-9]':
    IF:
    - LID: CLOSED
