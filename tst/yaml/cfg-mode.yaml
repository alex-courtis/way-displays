# $schema: ../../schema/cfg-2.0.0.yaml
MODE:
  width_height_hz:  # first one is overwritten
    WIDTH: 1920
    HEIGHT: 1080
    HZ: 12.34

  max_overrides:
    WIDTH: 1280
    HEIGHT: 720
    HZ: 60
    MAX: TRUE

  max_overrides_max_pref:
    WIDTH: 1024
    HEIGHT: 768
    HZ: 85
    MAX: TRUE
    MAX_PREFERRED_REFRESH: TRUE

  max_pref_overrides:
    WIDTH: 640
    HEIGHT: 480
    HZ: 30
    MAX_PREFERRED_REFRESH: TRUE

  max_only:
    MAX: TRUE

  max_pref_only:
    MAX_PREFERRED_REFRESH: TRUE

  '!(badpat':
    MAX: TRUE

  unknown_key:
    WIDTH: 800
    HEIGHT: 600
    FOO: bar

  empty_str:

  str_width:
    WIDTH: wid

  str_height:
    HEIGHT: hei

  str_hz:
    HZ: hzz

  str_max:
    MAX: maxx

  str_max_pref:
    MAX_PREFERRED_REFRESH: maxp

  seq_width:  
    WIDTH:
      -

  seq_height:  
    HEIGHT:
      -

  seq_hz:  
    HZ:
      -

  seq_max:  
    MAX:
      -

  seq_max_pref:  
    MAX_PREFERRED_REFRESH:
      -
