# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema

$schema: https://json-schema.org/draft/2020-12/schema
$id: https://raw.githubusercontent.com/alex-courtis/way-displays/refs/heads/master/schema/ipc-request-2.0.0-SNAPSHOT.yaml

title: way-displays IPC
description: |-
  Schema for https://github.com/alex-courtis/way-displays IPC

type: object
properties:

  OP:
    $ref: 'types-2.0.0-SNAPSHOT.yaml#/$defs/ipc_op'

  LOG_THRESHOLD:
    $ref: 'types-2.0.0-SNAPSHOT.yaml#/$defs/log_threshold'
    default: INFO
    description: |-
      Applies to response messages

  CFG:
    $ref: 'cfg-2.0.0-SNAPSHOT.yaml'
    description: |-
      Configuration deltas

additionalProperties: false
