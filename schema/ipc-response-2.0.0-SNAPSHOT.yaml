# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema

$schema: https://json-schema.org/draft/2020-12/schema
$id: https://raw.githubusercontent.com/alex-courtis/way-displays/refs/heads/master/schema/ipc-response-2.0.0-SNAPSHOT.yaml

title: way-displays IPC response
description: |-
  Schema for https://github.com/alex-courtis/way-displays IPC response

# TODO schema: oneOf is not working with yamlls, split into two
# TODO schema: why do sequenced responses exist?
oneOf:
  - $ref: "#$defs/response"
  - type: array
    items:
      $ref: "#$defs/response"

$defs:
  response:
    type: object
    properties:
      DONE:
        type: boolean
        description: |-
          Operation complete, no further data.

      CFG:
        $ref: "cfg-2.0.0-SNAPSHOT.yaml"

      STATE:
        type: object
        properties:
          LID:
            $ref: "types-2.0.0-SNAPSHOT.yaml#/$defs/lid"
          HEADS:
            type: array
            items:
              $ref: "types-2.0.0-SNAPSHOT.yaml#/$defs/head"
        additionalProperties: false

      MESSAGES:
        type: array
        items:
          $ref: "types-2.0.0-SNAPSHOT.yaml#/$defs/ipc_message"

      RC:
        type: integer
        description: |-
          0: success
          1: warning
          2: error
          11: bad request
          12: bad response
          13: request in progress
    additionalProperties: false
